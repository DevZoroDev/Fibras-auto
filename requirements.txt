# --- Interfaz gráfica ---
customtkinter==5.2.2

# --- OCR ---
# El motor invoca el binario de Tesseract por subprocess (no se usa pytesseract,
# que falla al decodificar el stderr de Tesseract en locales no-UTF8).
Pillow==10.4.0
opencv-python-headless==4.10.0.84
numpy==2.0.2

# --- Google Sheets API (OAuth de usuario) ---
gspread==6.1.2
google-auth==2.34.0
google-auth-oauthlib==1.4.0

# --- Configuración ---
python-dotenv==1.0.1

# --- Empaquetado ---
pyinstaller==6.10.0
